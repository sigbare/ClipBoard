namespace ClipBoard.Models;

public enum UserChoice
{
    ServerStart = 1,
    ConnectToServer = 2,
    SendTextToClipboard = 3,
    ShowConnectionStatus = 4,
    Disconnect = 5,
    Exit = 6,
    UnSupported = 7,
}

